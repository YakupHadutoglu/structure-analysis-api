import { zipParser } from './zipParser.service';
import { Express } from 'express';

export const fileStructureAnalysis = async (file: string | Express.Multer.File): Promise<void> => {
    return ;
}
